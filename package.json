{
  "name": "worktree-helper",
  "displayName": "Worktree Helper",
  "description": "Makes each git worktree window visually distinct and context-aware: titlebar color, branch/color env vars, and auto-opened terminals.",
  "version": "0.5.1",
  "publisher": "simonhowey",
  "license": "MIT",
  "repository": "https://github.com/simonhowey/worktree-helper-extension",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "keywords": [
    "git",
    "worktree",
    "titlebar",
    "terminal",
    "environment"
  ],
  "main": "./dist/extension.js",
  "extensionKind": [
    "workspace",
    "ui"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "worktreeHelper.reapply",
        "title": "Worktree Helper: Re-apply Config"
      },
      {
        "command": "worktreeHelper.clean",
        "title": "Worktree Helper: Clean Up This Window"
      },
      {
        "command": "worktreeHelper.pickColor",
        "title": "Worktree Helper: Pick Titlebar Color"
      },
      {
        "command": "worktreeHelper.runSetup",
        "title": "Worktree Helper: Run Setup Commands"
      },
      {
        "command": "worktreeHelper.stayInWsl",
        "title": "Worktree Helper: Stay in WSL (pause auto-container)"
      },
      {
        "command": "worktreeHelper.resumeAutoContainer",
        "title": "Worktree Helper: Resume auto-container"
      }
    ],
    "configuration": {
      "title": "Worktree Helper",
      "properties": {
        "worktreeHelper.autoApply": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Automatically apply worktree config (color, env, terminals) when a linked worktree window is opened."
        },
        "worktreeHelper.applyToMainWorktree": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Also apply config to the primary working tree, not just linked worktrees."
        },
        "worktreeHelper.settingsTemplate": {
          "type": "string",
          "default": ".vscode/settings.shared.json",
          "markdownDescription": "Workspace-relative path of a **tracked** settings template. When the file exists, its top-level keys are deep-merged into (untracked, gitignored) `.vscode/settings.json` on window open and whenever the template changes — template wins per key, keys it doesn't define (e.g. titlebar colors) are left alone, and a `null` value deletes the key. Runs in every trusted window, not just worktrees, since the generated `settings.json` is the only settings file VS Code reads. Empty disables."
        },
        "worktreeHelper.autoReopenInContainer": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "When a worktree window opens outside a container and the folder has a dev container config, automatically run *Dev Containers: Reopen in Container*. Best enabled per-repo in the checked-in `.vscode/settings.json`. Note: while this is on, *Reopen Folder Locally* will bounce straight back into the container — disable the setting first to work locally."
        },
        "worktreeHelper.setupCommands": {
          "type": "array",
          "default": [],
          "markdownDescription": "Commands to run **once** when a worktree is first set up (e.g. `npm install`, copying untracked config). They run sequentially in the worktree folder as VS Code tasks, stopping at the first failure, and re-run on the next open until they all succeed. Unlike `#worktreeHelper.terminals#`, these are expected to finish — use terminals for long-running processes like dev servers. Re-run on demand with the *Worktree Helper: Run Setup Commands* command.",
          "items": {
            "type": "string"
          }
        },
        "worktreeHelper.runSetupInDevContainer": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Run `#worktreeHelper.setupCommands#` in dev-container windows too. Off by default because the container's `postCreateCommand` normally owns bootstrap there. The *Run Setup Commands* command always works regardless."
        },
        "worktreeHelper.terminals": {
          "type": "array",
          "default": [],
          "markdownDescription": "Terminals to open in the worktree. Each runs in the worktree folder, then executes `command`. Omit `command` (or leave it empty) to open a plain terminal with nothing run.",
          "items": {
            "type": "object",
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "type": "string",
                "description": "Terminal tab name."
              },
              "command": {
                "type": "string",
                "default": "",
                "description": "Command to run when the terminal opens. Omit or leave empty to open a plain terminal."
              }
            }
          }
        },
        "worktreeHelper.palette": {
          "type": "array",
          "default": [
            "#95554b",
            "#876129",
            "#637032",
            "#2f795b",
            "#0c7684",
            "#446b9a",
            "#705d94",
            "#8c5475"
          ],
          "markdownDescription": "Candidate titlebar background colors (hex). New worktrees pick the most distinct unused color from this list.",
          "items": {
            "type": "string",
            "pattern": "^#([0-9a-fA-F]{6})$"
          }
        },
        "worktreeHelper.colorOverride": {
          "type": "string",
          "default": "",
          "pattern": "^(#([0-9a-fA-F]{6}))?$",
          "markdownDescription": "Force a specific titlebar background color (hex) for this workspace. Leave empty to auto-pick."
        },
        "worktreeHelper.injectEnv": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Inject `GIT_BRANCH` and the titlebar color into integrated terminals."
        },
        "worktreeHelper.writeEnvFile": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Write a git-excluded `.env` file at the worktree root so debuggers and tasks can read the vars."
        },
        "worktreeHelper.branchEnvVar": {
          "type": "string",
          "default": "GIT_BRANCH",
          "description": "Environment variable name holding the branch name."
        },
        "worktreeHelper.colorEnvVar": {
          "type": "string",
          "default": "WORKTREE_COLOR",
          "description": "Environment variable name holding the titlebar color."
        },
        "worktreeHelper.openTerminals": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Open the configured `worktreeHelper.terminals` when a worktree is set up."
        },
        "worktreeHelper.gitExcludeWrites": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Add files we write (`.vscode/settings.json`, `.env`) to `.git/info/exclude` so they don't pollute git."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.mjs --production",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "vsce package --no-dependencies",
    "reinstall": "vsce package --no-dependencies -o worktree-helper.vsix && code --install-extension worktree-helper.vsix --force",
    "install:win": "cp worktree-helper.vsix /mnt/c/Windows/Temp/wth.vsix && powershell.exe -NoProfile -Command \"cd C:\\Windows\\Temp; code --install-extension wth.vsix --force\"",
    "reinstall:win": "vsce package --no-dependencies -o worktree-helper.vsix && npm run install:win",
    "reinstall:all": "vsce package --no-dependencies -o worktree-helper.vsix && code --install-extension worktree-helper.vsix --force && npm run install:win"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.103.0",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.24.0",
    "jsonc-parser": "^3.3.1",
    "typescript": "^5.6.0"
  }
}
